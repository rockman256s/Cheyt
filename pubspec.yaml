name: weight_calculator
description: Weight Calculator based on pressure measurements
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flet: ^0.19.0
  sqlite3: ^1.3.0

flutter:
  assets:
    - assets/
  uses-material-design: true
